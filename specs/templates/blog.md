---
block: blog
fixture: /drafts/fixtures/templates/blog.plain.html
tags: [layout, responsive]
---

## Load and decoration

- The template loads and decorates without console errors
- The page body has the `blog` class

## Sidebar

- An archives sidebar is automatically added to the page
- The sidebar contains a simple archives block with categories and date groups

## Layout

- On desktop (1200px), the page displays a two-column layout with main content on the left and sidebar on the right
- On mobile (375px), the layout stacks vertically with main content above the sidebar
